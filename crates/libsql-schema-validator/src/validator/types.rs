// TAG: surface=database owner=platform-team rule=DB-001 test-coverage=unit
//! Type definitions for schema validation.
//!
//! The original `validator.rs` did not define any types locally;
//! all type definitions live in `crate::types`.
